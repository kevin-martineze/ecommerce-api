engine-strict=true
# Proyecto personal: registro público, no el CodeArtifact de Pangea que está
# configurado a nivel de usuario en ~/.npmrc. Sin esta línea, `pnpm install`
# falla con 401 contra un registro privado que no tiene estos paquetes.
registry=https://registry.npmjs.org/

